apiVersion: v1
kind: Service
metadata:
  name: mysql-db
  namespace: sales-db-ns
spec:
  selector:
    app.kubernetes.io/name: mariadb
    app.kubernetes.io/component: primary
    app.kubernetes.io/instance: mysql-db
  ports:
    - port: 3306
      targetPort: 3306